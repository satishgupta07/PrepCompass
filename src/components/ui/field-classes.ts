// Shared Tailwind class strings for form controls, kept in one place so
// every input/label across the app (edit panels, modals, etc.) stays
// visually consistent. Import these instead of re-typing the class lists.

export const inputClasses =
  "w-full rounded-md border border-border bg-background px-3 py-2 text-sm text-foreground placeholder:text-muted/70 focus:outline-none focus:ring-2 focus:ring-accent";

export const labelClasses = "text-sm font-medium text-muted";
